/**
 * Shared formatting helpers for the Results + Controls panels.
 */

// TODO: formatPercent(value, decimals = 1)
//   - Convert a 0–1 number to a "XX.X%" string

// TODO: formatDimensions({ width, depth, height }, unit = 'mm')
//   - Render a compact "W × D × H unit" string

// TODO: formatWeight(value, unit = 'kg')
//   - Render weight with unit suffix

// TODO: formatDuration(ms)
//   - Render a compute duration (e.g. "1.23s", "850ms")

